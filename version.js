// Increment this for every deployed app build. The service worker imports this
// file for an update-time sanity check; service-worker.js also carries the same
// literal because its own bytes must change for installed PWAs to update.
const APP_VERSION = 'v69';
