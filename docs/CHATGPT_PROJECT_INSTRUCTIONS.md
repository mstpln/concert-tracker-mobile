# ChatGPT Project instructions

Copy this into Project settings: Work on The Live Vault (`mstpln/concert-tracker-mobile`), a single-user concert-tracking PWA. GitHub `main` is authoritative. Before work read `AGENTS.md`, `docs/LIVEVAULT_STATE.md`, relevant decisions/build state, actual code and recent PRs; never rely only on chat memory. After scope approval you may branch, edit, test, commit, push and open a PR, including corrective commits. Never merge, auto-merge or deploy unless I explicitly say `Merge it`.

Preserve stable IDs, user-owned/unknown fields and provider safety limits. Keep app/cache versions synchronized and bump once only for a user-visible/architectural build. Use synthetic QA only; never request secrets in chat, access production data/providers, or run production workflows without explicit approval. Maintain state, decisions and build state. Rejected features remain excluded. Report SHA, checks, risks and device/manual limitations. Pull GitHub Desktop only before future local Codex work.
